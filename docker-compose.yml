services:
  port-tracker:
    build: .
    network_mode: host
    volumes:
      - port-tracker-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000

volumes:
  port-tracker-data:
